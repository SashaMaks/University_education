## Максимов Александр ИТ-17-2024
### Вариант 9

# Лабораторная работа №3
## Задание 1
#### Диапазон оценок.
Измените сущность Студент из задачи 1.5.6. Необходимо гарантировать, что добавлять Студенту
можно только оценки в диапазоне от 2 до 5, при этом у Студента всегда можно узнать список
оценок

### Решение:
Используем метод "Student" из лабораторной работы 2 
#### поля
  1. `private String name;` - имя студента
  2. `private int[] grades;` - массив оценок студента
#### конструкторы 
  1. `public Student(String name)` - для создания студента без оценок, но с именем
  2. `public Student(String name, int... grades)` - для создания студента с именем и с хоть каким количеством оценок (или без них)
#### геттеры
  1. `public String getName()` - возвращает имя студена
  2. `public int[] getGrades()` - возвращает массив оценок студента
  3. `public int[] getGradesNoLinks()` - возвращает массив оценок без ссылки на студента у которого смотрят оценки
#### сеттеры
  1. `public void setName(String name)` - устанавливает новое имя студенту
  2. `public void setGrades(int[] grades)` - устанавливает новые оценки студенту
  3. `public void setGrade(int index, int grade)` - устанавливает по индексу новую оценку
#### преобразование в строку
  1. `public String toString()` - выводит объект в виде “имя: [оценка1, оценка2,…,оценкаN]”

В класс Checks предназначенный для проверки и ввода разных значений создадим метод `readGrade()`, колторый вызывает `readIntInRange()` - метод изначально определяющий грани диапазона (большую и меньшую) и затем проверяет, находится ли число в диапазоне, если да, то возвращает значение, иначе ошибка
<img width="788" height="287" alt="image" src="https://github.com/user-attachments/assets/8ed69d6b-3a31-4271-ab75-53916b10ee43" />

## Задание 2

#### Желтые страницы.
Необходимо разработать сущность ТелефонныйСправочник, который будет использоваться для
создания справочника со списком телефонов и имен, с целью поиска телефона по имени. Будем
считать достаточным хранение пар телефон-строка, например “89003337788 - Вася”.
Состояние сущности описывается набором пар “телефон – имя”, причем и телефон и имя являются
строками. В целях упрощения задачи будем считать, что одному имени всегда соответствует один
телефон, и один телефон соответствует одному имени.
Инициализация сущности выполняется без аргументов.
Поведение сущности описывают следующие действия:
- Добавление новой пары “телефон – имя” (как два отдельных аргумента). Конкретное местоположение добавленной пары не важно. Если добавляемое имя уже есть в списке, то имеющийся телефон заменяется на новый, и затем старый телефон возвращается из метода.
- Удаление значения. Для удаления необходимо указать имя удаляемого контакта, после чего удаляется вся пара.
- Получение значений. Для получения необходимо указать имя контакта, тогда возвращается его телефон.
- Может быть приведен к строке. Строка должна представлять собой полный перечень всех хранимых пар.
- Можно проверить есть ли конкретный телефон или конкретное имя в списке.
- Можно узнать текущее количество контактов.
- Можно запросить в виде массива:
o Все пары
o Все телефоны
o Все имена
- Можно получить массив всех имен, указав часть названия (начиная с первого символа).
Продемонстрируйте работоспособность решения на примерах.


#### Решение:
Создан класс PhoneDirectory в котором имеются:
#### поля
  1. `private Map<String, String> contacts;`- хранилище контактов, где ключ - имя, а значение - телефон
#### конструкторы
  1. `public PhoneDirectory()` - создание пустого телефонного справочника
  2. `public PhoneDirectory(String name, String phone)` - создание справочника с одним контактом (если name == null или phone == null, выдает ошибку)
#### геттеры
  1. `public String getPhone(String name)` - возвращает телефон по имени или null, если такого имени нет
  2. `public String[] getNamesStarting(String start)` - возвращает массив имён, начинающихся с указанного префикса, но если start == null, возвращает пустой массив
  3. `public int getContactsCount()` - возвращает количество контактов
#### дополнительные методы
  1. `public String addContact(String name, String phone)` - добавляет новую пару «имя–телефон» или заменяет телефон у существующего имени (возвращает строку с предыдущим телефоном, если он был), если телефон уже есть у другого контакта, выводит предупреждение и не добавляет
  2. `public boolean containName(String name)` - проверяет наличие имени в справочнике
  3. `public boolean containPhone(String phone)` - проверяет наличие телефона в справочнике
  4. `public void deletePair(String name)` - удаляет контакт по имени
####  методы получения данных в виде массивов
  1. `public String[] AllPhones()` - возвращает массив всех телефонов
  2. `public String[] AllNames()` - возвращает массив всех имён
  3. `public String[][] AllPairs()` - возвращает двумерный массив вида: [[имя1, телефон1], [имя2, телефон2], ...]
#### преобразование в строку
  1. `public String toString()` - возвращает строку, где каждый контакт записан в формате: "телефон - имя" (каждый контакт с новой строки)
<img width="786" height="420" alt="image" src="https://github.com/user-attachments/assets/c7bd8f33-6500-4cd1-afa4-38d1f38d0430" />
<img width="539" height="558" alt="image" src="https://github.com/user-attachments/assets/50441284-3cc5-4d76-8adf-9f5d87ad66f6" />

## Задание 3

#### Трехмерная точка.
Создайте такой подвид сущности Точка из задачи 1.1.1, которая будет иметь не две, а три
координаты на плоскости: X,Y,Z.

#### Решение: 
Используем класс Point из лабораторной работы 2 и добавим новую координату Z, теперь он будет классои "Point3D":
#### поля
  1. `private double x;` - координата x
  2. `private double y;` - координата y
  3. `private double z;` - координата z
#### конструкторы 
  1. `public Point()` - для создания точки в координатах 0;0;0
  2. `public Point(double x, double y, double z)` - для создания точки по координатам
  3. `public Point(Point point)` - для создания точки по данной точке
#### геттеры
  1. `public double getX()` - возвращает координату x
  2. `public double getY()` - возвращает координату y
  3. `public double getZ()` - возвращает координату z
#### сеттеры
  1. `public void setX(double x)` - устанавливает координату x
  2. `public void setY(double y)` - устанавливает координату y
  3. `public void setY(double z)` - устанавливает координату z
  4. `public void setPoint(double x,double y,double z)` - устанавливает координаты x, y, z 
#### преобразование в строку
  1. `public String toString()` - выводит объект в виде “{X;Y;Z}”
<img width="811" height="223" alt="image" src="https://github.com/user-attachments/assets/e150f3a3-53c7-4c0e-bf1e-1ddef9975b44" />

## Задание 4

#### Фигуры.
Необходимо продумать структуру и организацию следующих геометрических фигур:
1. Круг. Задается точкой координат центра и радиусом.
2. Квадрат. Рекомендуется взять квадрат полученный в задаче 2.1.3
3. Прямоугольник. Задается точкой координат левого верхнего угла и двумя сторонами.
4. Треугольник. Задается тремя точками координат.
Для каждой фигуры обязательно должна быть возможность рассчитать её площадь

#### Решение: 
Создадим класс "Circle" круг:
#### поля
  1. `private Point center;` - центр круга
  2. `private double radius;` - радиус круга
#### конструкторы 
  1. `public Circle(Point center, double radius)` - для создания круга с точкой центра и радиусом
  2. `public Circle(double centerX, double centerY, double radius)` - для создания круга с точкой центра заданной координатами и радиусом
#### геттеры
  1. `public Point getCenter()` - возвращает точку центра круга
  2. `public double getRadius()` - возвращает радиус круга
  3. `public String getShapeType()` - возвращает тип фигуры
#### сеттеры
  1. `public void setCenter(Point center)` - устанавливает точку центра в данной точке8
  2. `public void setCenter(double centerX, double centerY)` - устанавливает точку центра в данных координатах
  3. `public void setRadius(double radius)` - устанавливает радиус для круга
#### дополнительные методы
  1. `public double calculateArea()` - вычисляет площвдь фигуры
#### преобразование в строку
  1. `public String toString()` - выводит объект в виде “Круг с центром {X;Y}, с радиусом {radius}, площадь {calculateArea()}”

Создадим класс "Square" квадрат:
#### поля
  1. `private Point topLeft;` - точка от которой строится квадрат (верхняя левая)
  2. `private double side;` - сторона квадрата
#### конструкторы 
  1. `public Square(Point topLeft, double side` - для создания квадрата с точкой и стороной
  2. `public Square(double topLeftX, double topLeftY, double side)` - для создания квадрата с точкой заданной координатами и стороной
#### геттеры
  1. `public Point getTopLeft()` - возвращает верхнюю левую точку квадрата
  2. `public double getSide()` - возвращает сторону квадрата
  3. `public Polyline getVerticesPolyline()` - возвращает ломаную формы квадрата
  4. `public Point[] getVertices()` - возвращает массив точек углов квадрата
  5. `public String getShapeType()` - возвращает тип фигуры
#### сеттеры
  1. `public void setTopLeft(Point topLeft)` - устанавливает точку начала квадрата
  2. `public void setTopLeft(double topLeftX, double topLeftY)` - устанавливает точку начала квадрата в данных координатах
  3. `public void setSide(double side)` - устанавливает сторону для квадрата
#### дополнительные методы
  1. `public double calculateArea()` - вычисляет площвдь фигуры
#### преобразование в строку
  1. `public String toString()` - выводит объект в виде “Квадрат в точке {X;Y}, со стороной {ышву}, площадь {calculateArea()}”

Создадим класс "Rectangle" прямоугольник:
#### поля
  1. `private Point topLeft;` - точка от которой строится прямоугольник (верхняя левая)
  2. `private double sideA;` - первая сторона прямоугольника
  3. `private double sideB;` - вторая сторона прямоугольника
#### конструкторы 
  1. `public Rectangle(Point topLeft, double sideA, double sideB)` - для создания прямоугольника с точкой и сторонами
  2. `public Rectangle(double topLeftX, double topLeftY, double sideA, double sideB)` - для создания прямоугольника с точкой заданной координатами и сторонами
#### геттеры
  1. `public Point getTopLeft()` - возвращает верхнюю левую точку прямоугольника
  2. `public double getSideA()` - возвращает первую сторону прямоугольника
  3. `public double getSideB()` - возвращает вторую сторону прямоугольника
  4. `public String getShapeType()` - возвращает тип фигуры
#### сеттеры
  1. `public void setTopLeft(Point topLeft)` - устанавливает точку начала прямоугольника
  2. `public void setTopLeft(double topLeftX, double topLeftY)` - устанавливает точку начала прямоугольника в данных координатах
  3. `public void setSideA(double sideA)` - устанавливает одну сторону для прямоугольника
  4. `public void setSideB(double sideB)` - устанавливает вторую сторону для прямоугольника
#### дополнительные методы
  1. `public double calculateArea()` - вычисляет площвдь фигуры
#### преобразование в строку
  1. `public String toString()` - выводит объект в виде “Прямоугольник в точке {X;Y}, со сторонами {sideA} {sideB}, площадь {calculateArea()}”

Создадим треугольник:
#### поля
  1. `private Point pointA;` - первая точка треугольника
  2. `private Point pointB;` - вторая точка треугольника
  3. `private Point pointC;` - третья точка треугольника
#### конструкторы 
  1. `public Triangle(Point pointA, Point pointB, Point pointC)` - создает треугольник по 3 точкам
  2. `public Triangle(double x1, double y1, double x2, double y2, double x3, double y3)` - создает треугольник по координатам 3 точек
#### геттеры
  1. `public Point getPointA()` - возвращает первую точку треугольника
  2. `public Point getPointB()` - возвращает первую точку треугольника
  3. `public Point getPointC()` - возвращает первую точку треугольника
  4. `public String getShapeType()` - возвращает тип фигуры
#### сеттеры
  1. `public void setPointA(Point pointA)` - устанавливает первую точку треугольника
  2. `public void setPointA(Point pointB` - устанавливает вторую точку треугольника
  3. `public void setPointA(Point pointC)` - устанавливает третью точку треугольника
#### дополнительные методы
  1. `public double calculateArea()` - вычисляет площвдь фигуры
  2. `private boolean isCollinear()` - возвращает true, если треугольник не лежит на одной плоскости, иначе false
#### преобразование в строку
  1. `public String toString()` - выводит объект в виде “Треугольник с точками A=" + {pointA} + ", B=" + {pointB} + ", C=" + {pointC} + ", площадь " + {calculateArea()}”

<img width="769" height="484" alt="image" src="https://github.com/user-attachments/assets/415ab755-4b8e-41eb-8fc1-e8eb58cf27be" />
<img width="745" height="607" alt="image" src="https://github.com/user-attachments/assets/3496eedb-6f4b-4037-8eef-0ff110412d51" />

## Задание 5.3

#### Общая площадь.
Разработайте метод, который принимает набор фигур из задачи 2.4.4 и считает их общую
площадь (без учета возможного перекрытия фигурами друг друга). Продемонстрируйте
работоспособность метода, передав туда несколько кругов и квадратов.

#### Решение: 
Создадим абстрактный класс "Shape", в нем должны быть:
  1. `public abstract String getShapeType();` - обязует добавить всем классам с объявленным абстракным классом вывод вида фигуры
  2. `public abstract double calculateArea();` - обязует добавить всем классам с объявленным абстракным классом вычисление площади

Добавим его к каждому классу который мы сделали в задании 4

создадим класс "GeometryUtils" с методом `public static double calculateTotalArea(Collection<Shape> shapes)` который будет считать суммарную площадь всех фигур с абстрактным классом Shape

<img width="560" height="662" alt="image" src="https://github.com/user-attachments/assets/f2318fe2-8005-4f70-b50b-033ff2495d2f" />

## Задание 5.6

#### Замкнутый квадрат.
Измените сущность Квадрат из задачи 2.4.4 таким образом что бы она возвращала не обычную
ломаную линию, а замкнутую ломаную линию из задачи 2.3.2. При этом сигнатура метода,
возвращающую Ломаную, должна остаться без изменений.

#### Решение: 
Добавим к классу квадрат:
- геттер который будет возвращать обычную ломанную, конец которой создаст класс замкнутая ломанная, которая проверяет, равна ли последняя данная точка первой или нет, елси нет то он создаст сам эту точку

 <img width="817" height="286" alt="image" src="https://github.com/user-attachments/assets/b6b9af04-e52e-4895-829f-d80cfdfcd0ea" />

## Задание 6
#### Сравнение точек.
Измените сущность Точка из задачи 1.4.1. Переопределите метод сравнения объектов по
состоянию таким образом, чтобы две точки считались одинаковыми тогда, когда они
расположены в одинаковых координатах.

#### Решение:
В класс Точка добавим метод, который сначала проверит, не ссылается ли объект сам на себя, если да то ну точки точно равны, если точка пустая или же класс подаваемый методу не точка, то они не равны, и уже дальше сравниваем координаты точек

<img width="783" height="486" alt="image" src="https://github.com/user-attachments/assets/9790f2f4-3de0-43a2-99c3-456d19ca2b14" />

## Задание 7.1

#### Навести порядок.
Данная задача предполагает реорганизацию ранее написанных классов. Расположите все ранее
написанные классы по пакетам таким образом, чтобы логически близкие классы оказались
сгруппированы друг с другом. Имена пакетов должны иметь как минимум трехсоставную форму,
вида: ru.surname.type. Вместо surname следует подставить свою фамилию, а вместо type
подставить название логического блока. Например, классы описывающие точку, линию, ломаную
линию, фигуру, квадрат, треугольник, круг и прямоугольник можно расположить в пакете
ru.surname.geometry. 

<img width="277" height="593" alt="image" src="https://github.com/user-attachments/assets/f9df8e4f-5613-4bf9-8299-23e80625569c" />

#### Решение:


## Задание 7.2

#### Главный метод.
Создайте пакет ru.surname.main (вместо surname необходимо подставить собственную
фамилию) в котором расположить класс с точкой входа в исполнение программы (public static void
main). Также следует проверить, что ни в одном другом пакете нет классов имеющих точку входа
в исполнение программы. В этом же пакете необходимо расположить класс (или интерфейс) с
методами из задач блока 2.5 и продемонстрировать их работоспособность. 

#### Решение: 
Перенесем в мейн задачку со студентом и покажем что его даже не надо импортировать как другие классы сейчас, которые находятся в других папках

<img width="792" height="138" alt="image" src="https://github.com/user-attachments/assets/2865ff4f-b349-433f-b26e-3a7909567518" />
<img width="299" height="124" alt="image" src="https://github.com/user-attachments/assets/d61f92e2-5458-4454-85c7-93c63e9bb121" />

## Задание 7.3

#### Возведение в степень.
Создайте метод принимающий две строки, в которых будут записаны числа X и Y. Возвращает
метод результат возведения X в степень Y. Для преобразования строки в число следует
использовать метод Integer.parseInt, а для возведения в степень метод Math.pow. Вызовите
разработанный метод передав туда параметры командной строки полученные точкой входа в
программу. Реализуйте метод так, что бы для возведения в степень и преобразования строки
использовались короткие имена статических методов
#### Решение: 
Создадим класс калькулятор который принимает значения из командной строки и возвращает уже степень Y числа X 

<img width="815" height="172" alt="image" src="https://github.com/user-attachments/assets/52fa7805-9e79-4041-a2af-c03f8d10ce57" />
<img width="791" height="140" alt="image" src="https://github.com/user-attachments/assets/fff06f54-5094-48aa-bdb9-e90e72507bc2" />

## Задание 8

#### Клонирование точки.
Измените сущность Точка из 3.6.2.Переопределите метод клонирования, унаследованный от
класса Object, таким образом, чтобы при его вызове возвращался новый объект Точки, значения
полей которого будут копиями оригинальной Точки.

#### Решение: 
Добавим в класс точка protected метод, который не получится вызвать из класса который в лругой категории, только из категории где и находится точка

<img width="579" height="456" alt="image" src="https://github.com/user-attachments/assets/47c13369-3740-4245-88c5-e5e63caab731" />
<img width="561" height="214" alt="image" src="https://github.com/user-attachments/assets/f4c53ec4-fc12-43b5-91af-9c6e8f00c0a7" />
