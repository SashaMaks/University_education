## Максимов Александр ИТ-17-2024
### Вариант 9

# Лабораторная работа №4
## Задание 1.3
#### Сравнимое.
Создайте ссылочный тип Сравнимое, гарантирующий наличие по данной ссылке метода со
следующими характеристиками:
- Называется “сравнить”
- Принимает объект.
- Тип принимаемого объекта может быть изменен без изменения самого Сравнимого.
- Возвращает целое число.

#### Решение:
Создадим интерфейс ```Comparable```, который принимает объекты, а все классы которые будут реализовывать интерфейс должны будут написать метод compare который возвращает ```int``` из принимаемого объекта

Для тестирования сделаем класс ```Number``` в котором реализуем ```Comparable\<Number>```:
#### поля
  1. `private int number;` - хранимое число
#### конструкторы 
  1. `public Number(int number)` - для создания объекта Число по числу
#### геттеры
  1. `public int getNumber()` - возвращает число
#### сеттеры
  1. `public int setNumber()` - устанавливает значение в число
#### метод для сравнения
  1. `public int compare(Number other)` - выводит 1, если объект который вызвал, больше чем подаваемый, -1 если наоборот и 0 если равны
#### преобразование в строку
  1. `public String toString()` - выводит объект в виде “Число: {number}”

## Задание 1.4

#### Сравнимый студент.
Создайте сущность Студент из задачи 2.1.9. Выполните реализацию Студентом метода “сравнить”
из задачи 3.1.3. таким образом, чтобы:
- Если средняя оценка текущего студента больше, чем у того с которым выполняется сравнение – возвращается 1.
- Если средние оценки сравниваемых студентов одинаковы – возвращается 0.
- Если средняя оценка текущего студента меньше, чем у того с которым выполняется сравнение – возвращается -1
Создайте двух студентов и сравните их между собой.
#### Решение:
Используем шаги из предыдущего задания, но в классе уже будем реализовывать интерфейс Comparable\<Student> 

в методе сравнения сравним средние оценки студентов (метод для подсчета средней оценки был сделан ранее в задачах) и выведем если объект который вызвал больше чем подавааемый, -1 если наоборот и 0 если равны

## Задание 2

#### Начало отсчета.
Создайте метод, принимающий Коробку из задачи 3.1.1, и кладет в неё трехмерную точку
координат (из задачи 2.1.5) с произвольными значениями. Метод должен позволять передавать
Коробку с более чем одним видом параметризации.

#### Решение:
Создадим класс коробку в которой находится:
- конструктор с пустым и с содержимым (любой объект)
- геттер объекта лежащего в коробке
- сеттер объекта в коробку
- метод проверки заполнена ли коробка
- преобразование в строку

Добавим туда метод который принимает коробку любого типа, и помещает туда 3Дточку

## Задание 3.1

#### Функция.
Разработайте такой метод, который будет принимать список значений типа T, и объект имеющий
единственный метод apply. Данный метод надо применить к каждому элементу списка, и вернуть
новый список значений типа P, при этом типы T и P могут совпадать, а могут не совпадать.

#### Решение:
Создаем интерфейс Функция в котором прописано что есть метод который преобразует объекты типа T в объекты типа R

Дальше напишем класс с методом function, который принимает список объектов T и люмбда выражение, и далее используя эти данные проходимся по списку обрабатывая каждый его эллемент, а обработанные значения складываем в обработанный список, который уже и возвращаем из метода

## Задание 3.2

#### Фильтр.
Разработайте такой метод, который будет принимать список значений типа T и объект имеющий
единственный метод test (принимает T и возвращает boolean). Верните новый список типа T, из
которого удалены все значения не прошедшие проверку условием.

#### Решение:
Создаем интерфейс Фильтр в котором прописано что есть метод, который проверяет объекты типа T и возвращает true или false в зависимости от того, проходит ли объект проверку.

Дальше напишем класс с методом filter, который принимает список объектов T и лямбда выражение, и далее используя эти данные проходимся по списку проверяя каждый элемент, а элементы которые прошли проверку складываем в новый список, который уже и возвращаем из метода.

## Задание 3.3

#### Сокращение.
Разработайте такой метод, который будет принимать список значений типа T и способ с помощью
которого список значений можно свести к одному значению типа T, которое и возвращается из
метода.
Далее необходимо изменить разработанный метод таким образом, чтобы данный метод
гарантированно не возвращал null и не выбрасывал ошибок в том случае, если исходный список
пуст

#### Решение:
Создаем интерфейс Сокращение в котором прописано что есть метод, который берет текущий накопленный результат и следующий элемент, и возвращает новый накопленный результат.

Дальше напишем класс с методом reduce, который принимает список объектов T, начальное значение и лямбда выражение, и далее используя эти данные проходимся по списку накапливая результат, применяя лямбда к каждому элементу вместе с текущим накопленным значением, а в конце возвращаем финальный накопленный результат.

Также изменяем метод, чтобы он гарантированно не возвращал null и не падал с ошибкой если список пуст. Для этого проверяем в начале метода: если список пустой или null, то сразу возвращаем начальное значение без всякой обработки.

## Задание 3.4

#### Коллекционирование.
Разработайте такой метод, который будет возвращать коллекцию типа P со значениями типа T.
Данный метод будет принимать:
1. Список исходных значений
2. Способ создания результирующей коллекции
3. Способ передачи значений исходного списка в результирующую коллекцию

#### Решение:
Создаем интерфейс Коллекционирование в котором прописан метод, который добавляет элемент в коллекцию определенным способом.

Дальше напишем класс с методом collect, который принимает список исходных значений, пустую коллекцию для результатов и лямбда выражение, и далее используя эти данные проходимся по списку и с помощью лямбды добавляем каждый элемент в результирующую коллекцию тем способом, который указан в лямбде, а в конце возвращаем заполненную коллекцию.
